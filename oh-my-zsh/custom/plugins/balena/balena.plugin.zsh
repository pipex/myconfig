# TODO: change the prompt to show
# * current signed backend (balena-cloud, balena-staging, your own). Get from balenarc.yml or env vars
# * login status (green for logged in)
# * signed in user (maybe admin privileges)

# echo "hello"
